import React from "react";
import { guardSafeIcon } from "./contracts/guardSafeIcon";
import { guardUnsafeIcon } from "./contracts/guardUnsafeIcon";
import { IW{{name}}SvgProps } from ".";
import { {{name}}IconsList } from ".";

{{imports}}

/**
 * {{name}} collection
 */
export const W{{name}}IconsRaw = (props: IW{{name}}SvgProps) => {
  const icon = guardSafeIcon(props)
    ? props.icon
    : guardUnsafeIcon(props)
    ? props.icon_UNSAFE
    : undefined;
  const { className } = props;
  if (!icon) return <></>;

  return (
    <div className={className}>
      {{switch}}
    </div>
  );
};
